# 快速部署指南

## 方法一：使用 Netlify CLI（推荐，最快）

### 安装 Netlify CLI

```bash
# 使用 npm 安装（需要先安装 Node.js）
npm install -g netlify-cli
```

### 部署步骤

1. 打开命令行，进入 `netlify-publish` 文件夹：
```bash
cd netlify-publish
```

2. 登录 Netlify：
```bash
netlify login
```
（会在浏览器中打开登录页面）

3. 初始化并部署：
```bash
netlify deploy --prod
```

**优点：**
- ✅ 上传速度更快（使用命令行传输）
- ✅ 可以查看上传进度
- ✅ 支持断点续传
- ✅ 可以设置环境变量等高级功能

---

## 方法二：使用 Git 连接（最快，推荐用于长期项目）

### 步骤

1. **创建 Git 仓库**（如果还没有）：
```bash
cd netlify-publish
git init
git add .
git commit -m "Initial commit"
```

2. **推送到 GitHub/GitLab**：
   - 在 GitHub 创建新仓库
   - 按照提示推送代码

3. **在 Netlify 连接 Git**：
   - 访问 [Netlify](https://app.netlify.com)
   - 点击 "Add new site" → "Import an existing project"
   - 选择您的 Git 提供商（GitHub/GitLab/Bitbucket）
   - 选择仓库
   - 设置：
     - Build command: 留空（静态网站）
     - Publish directory: `.` 或留空

**优点：**
- ✅ 最快（CDN 加速）
- ✅ 自动部署（每次 push 自动更新）
- ✅ 版本控制
- ✅ 可以回滚到之前的版本

---

## 方法三：压缩后上传（比拖放快）

1. **压缩文件夹**：
   - 将 `netlify-publish` 文件夹压缩为 ZIP 文件
   - Windows: 右键文件夹 → "发送到" → "压缩(zipped)文件夹"

2. **上传 ZIP 文件**：
   - 访问 [Netlify Drop](https://app.netlify.com/drop)
   - 直接拖放 ZIP 文件（Netlify 会自动解压）

**优点：**
- ✅ 比拖放文件夹稍快
- ✅ 单文件上传更稳定

---

## 方法四：使用 Vercel（备选方案，速度也很快）

### 使用 Vercel CLI

```bash
# 安装 Vercel CLI
npm install -g vercel

# 进入文件夹
cd netlify-publish

# 部署
vercel --prod
```

**优点：**
- ✅ 部署速度很快
- ✅ 全球 CDN
- ✅ 免费 SSL

---

## 推荐方案对比

| 方法 | 速度 | 难度 | 推荐度 |
|------|------|------|--------|
| Netlify CLI | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Git 连接 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 压缩上传 | ⭐⭐⭐ | ⭐ | ⭐⭐⭐ |
| 拖放文件夹 | ⭐⭐ | ⭐ | ⭐⭐ |

## 最快方案：Git 连接

如果您有 GitHub 账号，强烈推荐使用 Git 连接方式：
1. 上传到 GitHub（通常很快）
2. Netlify 自动从 GitHub 拉取（CDN 加速，非常快）
3. 以后更新只需 push 代码，自动部署

